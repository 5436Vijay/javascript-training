

const number = ()=>{
    return Math.floor( Math.random()*10);
    //Math.random is use for printing the random value between 0 to 1.
    //Math.floor is use for printing the complete value not in fraction.
}
console.log(number());
